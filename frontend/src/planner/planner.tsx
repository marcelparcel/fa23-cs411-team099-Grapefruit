import React from 'react';
import './planner.css';

export default function PlannerView() {
    // two textboxes (inputs), one for starting stop, one for destination stop
    // maybe a dropdown for the textboxes (that show the stops), so its easier for the user to find
    // another textbox(?) for the user to select a date and time (do not include year bc our data only has 1 year..)
    // a button that submits the two picked stops to the database
    // wait for return..
    // returns a visualization of the routes that go to both stops (in the correct order) given date/time
    // also returns the time of departure for each stop for the route ?

    return (
        <div>
            {/* this is a placeholder */}
            <h1> </h1> 
        </div>
    );
}